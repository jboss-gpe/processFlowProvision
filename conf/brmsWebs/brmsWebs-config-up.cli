
#if(${org.jboss.processFlow.brmsWebs.clustered})

# the following fails on a slave node on eap6.0.1
# only way to resolve seems to be to kill host controller on slave node ..... then bring servers up
    batch
    /host=${serverIpAddr}/server-config=brms-webs/:start(server=brms-webs,blocking=true)
    run-batch
#end-if
