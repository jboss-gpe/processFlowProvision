package org.jboss.processFlow.tasks;

import org.jbpm.task.Task;
import org.jbpm.task.service.ContentData;

import static org.junit.Assert.assertEquals;
import javax.ejb.EJB;
import org.jboss.arquillian.container.test.api.Deployment;
import org.jboss.arquillian.junit.Arquillian;
import org.jboss.shrinkwrap.api.ShrinkWrap;
import org.jboss.shrinkwrap.api.spec.JavaArchive;
import org.jboss.shrinkwrap.api.asset.EmptyAsset;
import org.junit.Test;
import org.junit.runner.RunWith;
import javax.inject.Inject;

@RunWith(Arquillian.class)
public class HumanTaskServiceTest {

    @Deployment
    public static JavaArchive createDeployment() {
        return ShrinkWrap.create(JavaArchive.class)
            .addClass(org.apache.log4j.Logger.class)
            .addClass(org.drools.event.AbstractEventSupport.class)
            .addClass(org.drools.SystemEventListener.class)
            .addClass(org.drools.SystemEventListenerService.class)
            .addClass(org.drools.SystemEventListenerFactory.class)
            .addClass(org.jboss.processFlow.PFPBaseService.class)
            .addClass(org.jboss.processFlow.knowledgeService.IBaseKnowledgeSessionService.class)
            .addClass(org.jboss.processFlow.MockKnowledgeSessionService.class)
            .addClass(org.jboss.processFlow.tasks.event.PfpTaskEventSupport.class)
            .addClass(org.jbpm.task.query.TaskSummary.class)
            .addClass(org.jbpm.task.service.CannotAddTaskException.class)
            .addClass(org.jbpm.task.service.ContentData.class)
            .addClass(org.jbpm.task.service.EscalatedDeadlineHandler.class)
            .addClass(org.jbpm.task.service.FaultData.class)
            .addClass(org.jbpm.task.service.IllegalTaskStateException.class)
            .addClass(org.jbpm.task.service.IncorrectParametersException.class)
            .addClass(org.jbpm.task.service.Operation.class)
            .addClass(org.jbpm.task.service.PermissionDeniedException.class)
            .addClass(org.jbpm.task.service.TaskException.class)
            .addClass(org.jbpm.task.service.TaskService.class)
            .addClass(org.jbpm.task.service.TaskServiceSession.class)
            //.addClass(org.jbpm.task.service.persistence.TaskPersistenceManager.class)
            .addPackages(false, "org/jboss/processFlow/tasks")
            .addPackages(false, "org/jbpm/eventmessaging")
            .addPackages(false, "org/jbpm/task")
            .addPackages(false, "org/jbpm/task/event")
            //.addAsResource("META-INF/ejb-jar.xml", "META-INF/ejb-jar.xml")
            .addAsResource("META-INF/pfp-Taskorm.xml", "META-INF/pfp-Taskorm.xml")
            .addAsResource("META-INF/test-persistence.xml", "META-INF/persistence.xml")
            .addAsManifestResource(EmptyAsset.INSTANCE, "beans.xml");
    }

    @Inject
    HumanTaskService htService;

    @Test
    public void addTaskTest() {
        Task taskObj = new Task();
        ContentData inboundTaskVars = null;
        htService.addTask(taskObj, inboundTaskVars);
    }

}
