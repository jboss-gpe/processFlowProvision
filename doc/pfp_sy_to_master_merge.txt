1)  create separate module names/paths for drools/jbpm/mvel libraries that will conflict between SY and existing master branch PFP
2)  create a domain managed "syCore" worker server
    - use Switchyard's existing standalone.xml as a template
